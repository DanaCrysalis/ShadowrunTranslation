# Examples

`ui_test.tsv`      44-line UI translation, plain ASCII, for `hwpatch.py`.
                   Menus only -- see FINDINGS.md 5.5 for why.
`hi7.tsv`          the build that confirmed dialogue works: high-bit English
                   at the original line breaks, plus a Japanese/English/
                   Japanese sandwich. For `hipatch.py`.
`tail6.tsv`        a mid-line probe, for `tailpatch.py`.
`reflow_test.tsv`  a block-level rewrite, for `blockpatch.py`. Historical --
                   the reflow question turned out to be moot (FINDINGS 11.8).
